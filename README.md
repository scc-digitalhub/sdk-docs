# SDK Documentation

This is the documentation for the SDK. Browse it [here](https://scc-digitalhub.github.io/sdk-docs/).


## Development

1. Install dependencies:

```bash
pip install -r requirements.txt
```

2. Run the SDK locally:

```bash
mkdocs serve
```

or 

```bash
python -m mkdocs serve
```